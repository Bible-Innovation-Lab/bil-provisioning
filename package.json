{
  "name": "bil-provisioning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "redactor-sample": "bun scripts/redactor-sample.ts",
    "smoke": "bun run typecheck && bun run test && bun run redactor-sample"
  },
  "dependencies": {
    "@octokit/request": "^9.2.4",
    "@vercel/kv": "^3.0.0",
    "hono": "^4.6.20",
    "posthog-node": "^5.33.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^20",
    "@vercel/node": "^5.2.0",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
