{
  "name": "bil-provisioning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run --dir test --testNamePattern='unit|.unit'",
    "smoke": "vitest run --dir test --testPathPattern='unit'"
  },
  "dependencies": {
    "@octokit/request": "^9.2.4",
    "@vercel/kv": "^3.0.0",
    "hono": "^4.6.20",
    "posthog-node": "^5.33.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^20",
    "@vercel/node": "^5.2.0",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
